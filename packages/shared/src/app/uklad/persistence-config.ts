/** Stable storage namespace and schema versions for the application. */
export const APP_PERSISTENCE_PREFIX = "ebtest";

/** Version used while importing the pre-Uklad raw storage values. */
export const APP_PERSISTENCE_LEGACY_VERSION = 1;

/** Current version of the Uklad persistence envelope. */
export const APP_PERSISTENCE_VERSION = 2;
